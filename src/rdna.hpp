 #include <random>
 
char rdna() //This function accepts user input and outputs the DNA sequence
 {
	 std::random_device rd;
	 
	 char letterb[];
	 int length;
	 
	 std::uniform_int_distribution<> unifrm(1, 4);
	 
	 for (int n=0; n<10; n++)
	 {
	 `	std::cout<<
	 
